#include <stdio.h>
int main()
{
    int vajok, vajjo, vagfol, vagses;

    printf("Enter vajok:");
    scanf("%d", &vajok);

    printf("Enter vajjo:");
    scanf("%d", &vajjo);

    vagfol = vajjo / vajok;

    vagses = vajjo - (vagfol * vajok);

    printf("vagses holo %d", vagses);

    return 0;


}
