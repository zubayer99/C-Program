#include #stdio.h>

int main()

{
    int a, b, v, x, vagses;

    scanf("%d", &a);

    scanf("%d", &b);

    v = b / a;

    x = a * v;

    vagses = b - x;

    printf("Vagses holo %d", b - x);

    return 0;

}
